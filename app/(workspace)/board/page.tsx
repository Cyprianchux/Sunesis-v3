"use client";

import { useEffect, useRef, useState } from "react";
import { Button, Toast } from "../../../components/ui";
import { WorkspaceGuard } from "../../../components/workspace-guard";
import { getBoardEntries, saveBoardEntries } from "../../../lib/storage";
import type { BoardEntry } from "../../../lib/types";
import { makeId } from "../../../lib/utils";

export default function BoardPage() {
  const [content, setContent] = useState(""); const [entries, setEntries] = useState<BoardEntry[]>([]); const [light, setLight] = useState(false); const [fontSize, setFontSize] = useState(32); const [alignment, setAlignment] = useState<"left" | "center" | "right">("left"); const [notice, setNotice] = useState(""); const editorRef = useRef<HTMLTextAreaElement>(null);
  useEffect(() => { setEntries(getBoardEntries()); setLight(window.localStorage.getItem("sunesisWordLightMode") === "true"); setFontSize(Number(window.localStorage.getItem("sunesisWordFontSize")) || 32); }, []);
  const save = () => { if (!content.trim()) return; const next = [...entries, { id: makeId(), content: content.trim(), createdAt: new Date().toISOString() }]; setEntries(next); saveBoardEntries(next); setNotice("Write-up saved successfully."); };
  const removeLast = () => { if (!entries.length) return setNotice("No saved write-ups found."); const next = entries.slice(0, -1); setEntries(next); saveBoardEntries(next); setContent(""); };
  const clear = () => { setEntries([]); saveBoardEntries([]); setContent(""); };
  const handleEditorKeyDown = (event: React.KeyboardEvent<HTMLTextAreaElement>) => { if (event.key !== "Enter" || event.shiftKey) return; const editor = event.currentTarget; const beforeCursor = content.slice(0, editor.selectionStart); const lineStart = beforeCursor.lastIndexOf("\n") + 1; const currentLine = beforeCursor.slice(lineStart); if (currentLine === "• ") { event.preventDefault(); const next = `${content.slice(0, lineStart)}${content.slice(editor.selectionStart)}`; setContent(next); requestAnimationFrame(() => { editor.focus(); editor.setSelectionRange(lineStart, lineStart); }); return; } if (currentLine.startsWith("• ")) { event.preventDefault(); const next = `${content.slice(0, editor.selectionStart)}\n• ${content.slice(editor.selectionEnd)}`; setContent(next); const cursor = editor.selectionStart + 3; requestAnimationFrame(() => { editor.focus(); editor.setSelectionRange(cursor, cursor); }); } };
  const addBullet = () => { const editor = editorRef.current; const start = editor?.selectionStart ?? content.length; const prefix = content.slice(0, start); const suffix = content.slice(start); const next = `${prefix}${prefix && !prefix.endsWith("\n") ? "\n" : ""}• ${suffix}`; setContent(next); const cursor = start + (prefix && !prefix.endsWith("\n") ? 3 : 2); requestAnimationFrame(() => { editor?.focus(); editor?.setSelectionRange(cursor, cursor); }); };
  return <WorkspaceGuard><div className="mx-auto max-w-5xl px-6 py-10">{notice && <Toast message={notice} />}<header className="mb-8"><p className="text-xs font-bold uppercase tracking-[.15em] text-brick">Thinking space</p><h1 className="mt-2 text-4xl font-black text-maroon">Typing board</h1><p className="mt-2 text-muted">Write, save, and revisit your explanations.</p></header><div className="mb-4 flex flex-wrap gap-2"><button onClick={() => { const next = !light; setLight(next); window.localStorage.setItem("sunesisWordLightMode", String(next)); }} className="rounded-lg border border-line bg-white px-3 py-2 text-sm font-bold text-maroon">◐ Theme</button><button onClick={() => { const next = Math.max(20, fontSize - 4); setFontSize(next); window.localStorage.setItem("sunesisWordFontSize", String(next)); }} className="rounded-lg border border-line bg-white px-3 py-2 text-sm font-bold text-maroon">−</button><button onClick={() => { const next = fontSize + 4; setFontSize(next); window.localStorage.setItem("sunesisWordFontSize", String(next)); }} className="rounded-lg border border-line bg-white px-3 py-2 text-sm font-bold text-maroon">+</button><button aria-label="Align left" onClick={() => setAlignment("left")} className={`rounded-lg border border-line bg-white px-3 py-2 text-sm font-bold text-maroon ${alignment === "left" ? "ring-2 ring-tint" : ""}`}>☰</button><button aria-label="Align center" onClick={() => setAlignment("center")} className={`rounded-lg border border-line bg-white px-3 py-2 text-sm font-bold text-maroon ${alignment === "center" ? "ring-2 ring-tint" : ""}`}>≡</button><button aria-label="Align right" onClick={() => setAlignment("right")} className={`rounded-lg border border-line bg-white px-3 py-2 text-sm font-bold text-maroon ${alignment === "right" ? "ring-2 ring-tint" : ""}`}>☰</button><button onClick={addBullet} className="rounded-lg border border-line bg-white px-3 py-2 text-sm font-bold text-maroon">• Bullet</button></div><textarea ref={editorRef} autoFocus value={content} onChange={(e) => setContent(e.target.value)} onKeyDown={handleEditorKeyDown} spellCheck className={`min-h-[420px] w-full resize-y rounded-2xl border p-8 outline-none ${light ? "border-line bg-white text-ink" : "border-[#402528] bg-[#241013] text-white"} focus:ring-4 focus:ring-tint`} style={{ fontSize, textAlign: alignment }} placeholder="Start writing here..." /><p className="mt-3 text-xs text-muted">Enter = new line · Shift + Enter = save</p><div className="mt-5 flex flex-wrap gap-3"><Button onClick={save}>▣ Save write-up</Button><button onClick={removeLast} className="rounded-xl border border-line bg-white px-4 py-3 text-sm font-bold text-maroon">Delete last</button><button onClick={clear} className="rounded-xl border border-red-200 bg-white px-4 py-3 text-sm font-bold text-red-700">Clear all</button></div>{entries.length > 0 && <section className="mt-10 rounded-2xl border border-line bg-white p-6"><h2 className="font-bold text-maroon">Saved write-ups</h2><div className="mt-4 grid gap-3">{entries.map((entry) => <button key={entry.id} onClick={() => setContent(entry.content)} className="rounded-xl bg-soft p-4 text-left text-sm text-ink hover:ring-2 hover:ring-tint">{entry.content.slice(0, 100)}{entry.content.length > 100 ? "..." : ""}</button>)}</div></section>}</div></WorkspaceGuard>;
}
